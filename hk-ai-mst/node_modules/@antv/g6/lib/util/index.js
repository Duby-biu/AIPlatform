/**
 * @fileOverview util
 * @author huangtonger@aliyun.com
 */
var Util = {};

var MathUtil = require('./math');

var PathUtil = require('./path');

var BaseUtil = require('./base');

var DomUtil = require('./dom');

var GraphUtil = require('./graph');

var GraphicUtil = require('./graphic');

var AnimateUtil = require('./animate');

BaseUtil.mix(Util, BaseUtil, GraphUtil, GraphicUtil, DomUtil, PathUtil, MathUtil, AnimateUtil);
module.exports = Util;